# 🚀 部署指南

本專案建議使用 **Cloudflare Pages** 進行部署，提供免費、快速且穩定的靜態網站託管服務。

## 📋 部署前準備

### 必要條件

- GitHub 帳號
- Cloudflare 帳號（免費註冊：[cloudflare.com](https://dash.cloudflare.com/sign-up)）
- 已 Fork 或擁有本專案的 GitHub 倉庫

### 專案建置資訊

- **建置命令**: `npm run build`
- **輸出目錄**: `dist`
- **Node.js 版本**: >= 18.0.0
- **npm 版本**: >= 9.0.0

## 🌐 Cloudflare Pages 部署步驟

### 1. 登入 Cloudflare Dashboard

1. 前往 [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. 登入您的帳號（如果還沒有帳號，請先註冊）

### 2. 建立新專案

1. 在左側選單中，點擊 **Pages**
2. 點擊 **Create a project**
3. 選擇 **Connect to Git**
4. 授權 Cloudflare 存取您的 GitHub 帳號（如果尚未授權）
5. 選擇您的倉庫（`SocialCoachAI` 或您的 Fork）

### 3. 設定建置配置

在專案設定頁面中，配置以下資訊：

#### 基本設定

- **Project name**: `social-coach-ai`（或您喜歡的名稱）
- **Production branch**: `main`（或您的主要分支）

#### 建置設定

- **Framework preset**: `Vite`（或選擇 `None`）
- **Build command**: `npm run build`
- **Build output directory**: `dist`
- **Root directory**: `/`（留空或填寫 `/`）

#### 環境變數（可選）

如果需要設定環境變數（例如 `GEMINI_API_KEY`），可以在這裡設定：

> ⚠️ **注意**：建議不要在此設定 `GEMINI_API_KEY`，因為這是用戶自己的 API Key，應該在應用程式中設定。

### 4. 部署

1. 點擊 **Save and Deploy**
2. 等待建置完成（通常需要 1-3 分鐘）
3. 建置成功後，您會獲得一個 Cloudflare Pages 的 URL（例如：`https://your-project.pages.dev`）

### 5. 自訂網域（可選）

如果您有自己的網域：

1. 在專案設定中，點擊 **Custom domains**
2. 輸入您的網域
3. 按照指示設定 DNS 記錄
4. Cloudflare 會自動配置 SSL 憑證

## 🔄 自動部署

Cloudflare Pages 會自動監控您的 GitHub 倉庫：

- **每次推送到 `main` 分支**：自動觸發生產環境部署
- **每次建立 Pull Request**：自動建立預覽部署

### 預覽部署

當您建立 Pull Request 時，Cloudflare Pages 會自動建立一個預覽部署，讓您可以：
- 在合併前測試變更
- 與團隊分享預覽連結
- 確保變更正常運作

## 🛠️ 本地測試部署

在推送到 GitHub 之前，建議先在本地測試建置：

```bash
# 安裝依賴
npm install

# 建置專案
npm run build

# 預覽生產版本
npm run preview
```

訪問 `http://localhost:4173`（或終端顯示的 URL）來預覽建置結果。

## 📝 建置日誌與除錯

### 查看建置日誌

1. 在 Cloudflare Pages 專案頁面中
2. 點擊 **Deployments** 標籤
3. 選擇特定的部署
4. 查看建置日誌以診斷問題

### 常見問題

#### 建置失敗

**問題**: 建置失敗，顯示錯誤訊息

**解決方案**:
1. 檢查建置日誌中的錯誤訊息
2. 確認 `package.json` 中的依賴版本正確
3. 確認 Node.js 版本符合要求（>= 18.0.0）
4. 在本地執行 `npm run build` 測試

#### 404 錯誤

**問題**: 部署成功但訪問時出現 404

**解決方案**:
1. 確認 **Build output directory** 設定為 `dist`
2. 檢查 `dist` 目錄中是否有 `index.html`
3. 確認 Vite 配置正確

#### 環境變數未生效

**問題**: 設定的環境變數無法使用

**解決方案**:
1. 確認環境變數名稱正確
2. 重新部署專案
3. 檢查應用程式中的環境變數讀取方式

## 🔧 進階設定

### 設定 Node.js 版本

如果需要指定特定的 Node.js 版本，可以在專案根目錄建立 `.nvmrc` 檔案：

```
18.17.0
```

或在 Cloudflare Pages 的環境變數中設定 `NODE_VERSION`。

### 設定建置快取

Cloudflare Pages 會自動快取 `node_modules`，但如果需要自訂快取策略，可以在專案設定中配置。

### 設定重定向規則

如果需要設定重定向規則（例如 SPA 路由），可以在 `public/_redirects` 檔案中設定：

```
/*    /index.html   200
```

## 📊 效能優化建議

### 1. 啟用自動壓縮

Cloudflare Pages 會自動壓縮靜態資源，無需額外設定。

### 2. 使用 CDN

Cloudflare Pages 使用 Cloudflare 的全球 CDN，自動提供最佳效能。

### 3. 快取策略

- 靜態資源（JS、CSS）會自動設定長期快取
- HTML 檔案會設定短期快取

### 4. 圖片優化

考慮使用 Cloudflare Images 或 Cloudflare Image Resizing 來優化圖片載入。

## 🔐 安全性建議

1. **API Key 管理**：不要在環境變數中設定用戶的 API Key，應該在應用程式中讓用戶自行設定
2. **內容安全政策**：考慮設定 CSP headers
3. **HTTPS**：Cloudflare Pages 自動提供 HTTPS，無需額外設定

## 📚 相關資源

- [Cloudflare Pages 文件](https://developers.cloudflare.com/pages/)
- [Vite 部署指南](https://vitejs.dev/guide/static-deploy.html)
- [Cloudflare Pages 最佳實踐](https://developers.cloudflare.com/pages/platform/build-configuration/)

## 💬 需要幫助？

如果在部署過程中遇到問題：

1. 查看 [Cloudflare Pages 文件](https://developers.cloudflare.com/pages/)
2. 在 [Issues](https://github.com/mkhsu2002/SocialCoachAI/issues) 中搜尋相關問題
3. 建立新的 Issue 尋求協助

---

**祝您部署順利！** 🚀

